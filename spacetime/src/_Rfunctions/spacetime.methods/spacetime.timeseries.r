
  spacetime.timeseries = function( x, p ) {
    # construct a space-timeseries interpolation 

    if (0) {
      # for debugging / testing ...
      require(sp)
      require(gstat)
      require(INLA)
      data(meuse)
      x = meuse
      p=list()

    }
  
  



  }
