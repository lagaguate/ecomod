
  x = bio.db( "set" ) 
  y = bio.db( "det" ) 

  y = merge (y, x)
  

  condition.estimation.model = function( DS="saved", X=y ) {
    
    lwmodel = glm( log(mass) ~ log(len) + sex + spec,  
           
    return(x)
  }
      
  
